import React, { useEffect } from 'react';
// import { useHistory } from 'react-router-dom';

const Block = () => {
//   const history = useHistory();

//   useEffect(() => {
//     const unblock = history.block((location) => {
//       // Vérifie si l'URL à laquelle on tente de naviguer contient le lien spécifique
//       if (location.pathname.startsWith("/facture")) {
//         // Redirige vers le dashboard
//         history.push('/crm');
//         return false; // Empêche la navigation
//       }
//       return true; // Autorise la navigation pour d'autres URLs
//     });

//     return () => {
//       unblock(); // Désinscrit le block lorsque le composant est démonté
//     };
//   }, [history]);

//   return null; // Ce composant ne rend rien visuellement
};

export default  Block;